package net.time4j.format;

import java.util.Locale;

/* JADX INFO: loaded from: classes9.dex */
public interface u {
    String[] b(String str, Locale locale, v vVar, m mVar);

    boolean c(Locale locale);

    String[] d(String str, Locale locale, v vVar, m mVar);

    String[] e(String str, Locale locale, v vVar, m mVar, boolean z11);

    String[] g(String str, Locale locale, v vVar);

    String[] h(String str, Locale locale, v vVar, m mVar);

    boolean j(String str);
}
