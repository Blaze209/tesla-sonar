package net.time4j.format;

import java.util.Locale;

/* JADX INFO: loaded from: classes9.dex */
public interface x {
    String A(Locale locale, v vVar, n nVar);

    String C(Locale locale, boolean z11, n nVar);

    String D(Locale locale, boolean z11, n nVar);

    String b(Locale locale);

    String c(Locale locale, boolean z11, n nVar);

    String d(Locale locale, v vVar, n nVar);

    String e(Locale locale, boolean z11, n nVar);

    String h(Locale locale, v vVar, n nVar);

    String i(Locale locale, v vVar, n nVar);

    String k(Locale locale, v vVar, n nVar);

    String m(Locale locale, v vVar, int i11);

    String n(Locale locale, boolean z11, n nVar);

    String r(Locale locale, v vVar, n nVar);

    String u(Locale locale, v vVar, n nVar);

    String v(Locale locale, boolean z11, n nVar);

    String w(Locale locale, v vVar, n nVar);

    String x(Locale locale, v vVar, n nVar);

    String y(Locale locale, boolean z11, n nVar);

    String z(Locale locale, v vVar, n nVar);
}
